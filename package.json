{
  "name": "aeolus",
  "version": "2.0.0",
  "description": "Aeolus — 金融 Skill 平台",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"node backend/server.js\" \"pnpm --filter ./frontend run dev\"",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "pnpm --filter ./frontend run dev",
    "build": "pnpm --filter ./frontend run build",
    "preview": "pnpm --filter ./frontend run preview",
    "install:all": "pnpm install && pnpm --filter ./frontend install && pnpm --filter ./backend install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
